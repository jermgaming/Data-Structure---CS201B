# 3.5.1. Solve the Puzzle, Get the Bow

## Description
This C program is located at `Unit 3 - Stack/3.5 Essential Programs/3.5.1. Solve the Puzzle, Get the Bow.c`.

## Code
```c
// Type Content here...

#include<stdio.h>
#include<string.h>
int main(){
	char s[100];
	scanf("%s",s);
	int size=0;
	int n;
	scanf("%d",&n);
	while(s[size]!='\0'){
		size++;
	}
	//printf("%d",size);
	if((-n+size)<0){
		printf("Invalid position\n");
	}else{
		printf("%c",s[-n+size]);
	}
	//printf("%c",s[-n+size]);
	return 0;
}...
```

## Compilation
```bash
gcc "3.5.1. Solve the Puzzle, Get the Bow.c" -o 3.5.1. Solve the Puzzle, Get the Bow
```

## Usage
```bash
./3.5.1. Solve the Puzzle, Get the Bow
```

## Notes
- Auto-generated README (API key not configured for detailed analysis)
- Please add detailed documentation manually
