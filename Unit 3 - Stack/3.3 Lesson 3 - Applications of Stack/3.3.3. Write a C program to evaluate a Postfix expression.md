# 3.3.3. Write a C program to evaluate a Postfix expression

## Description
This C program is located at `Unit 3 - Stack/3.3 Lesson 3 - Applications of Stack/3.3.3. Write a C program to evaluate a Postfix expression.c`.

## Code
```c
#include <ctype.h>
#include <stdio.h>
#define STACK_MAX_SIZE 20
//Declare the required stack variables.
int arr[STACK_MAX_SIZE];
int top=-1;

//Return 1 if stack is empty else return 0.
int isEmpty() {
	if(top==-1){
		return 1;
	}else {
		return 0;
	}
}

//Push the character into stack
void push(int x) {
	arr[++top]=x;
}

//pop a character from stack
int pop() {
	return arr[top--];
}

void peek(){
	if(top==-1){
		printf("Stack is empty");
	}else {
		printf("%d \n",arr[top]);
	}
}
void evaluatePo...
```

## Compilation
```bash
gcc "3.3.3. Write a C program to evaluate a Postfix expression.c" -o 3.3.3. Write a C program to evaluate a Postfix expression
```

## Usage
```bash
./3.3.3. Write a C program to evaluate a Postfix expression
```

## Notes
- Auto-generated README (API key not configured for detailed analysis)
- Please add detailed documentation manually
