# 3.6.1. Implement Two Stacks in Single Array

## Description
This C program is located at `Unit 3 - Stack/3.6 Practice Programs/3.6.1. Implement Two Stacks in Single Array.c`.

## Code
```c
#include<stdio.h>

int main() {
    int n, q;
    scanf("%d%d", &n, &q);
    int top1 = -1;
    int top2 = n;
    int arr[n];
    
    while(q--) {
        int temp;
        scanf("%d", &temp);
        
        if(temp == 1) {
            int x;
            scanf("%d", &x);
            
            if(top1 + 1 < top2) {
                arr[++top1] = x;
            }
            
        } 
        else if(temp == 2) {  
            int x;
            scanf("%d", &x);
            
            if(...
```

## Compilation
```bash
gcc "3.6.1. Implement Two Stacks in Single Array.c" -o 3.6.1. Implement Two Stacks in Single Array
```

## Usage
```bash
./3.6.1. Implement Two Stacks in Single Array
```

## Notes
- Auto-generated README (API key not configured for detailed analysis)
- Please add detailed documentation manually
