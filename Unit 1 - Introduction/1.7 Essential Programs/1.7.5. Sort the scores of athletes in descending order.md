# 1.7.5. Sort the scores of athletes in descending order

## Description
This program implements 1.7.5. Sort the scores of athletes in descending order

## File Location
`Unit 1 - Introduction/1.7 Essential Programs/1.7.5. Sort the scores of athletes in descending order.c`

## Code Preview
```c
// write your code here
#include<stdio.h>
#include<stdlib.h>
void bubbleSort(int arr[],int n){
	for(int i=0;i<n;i++){
		for(int j=0;j<n-i-1;j++){
			if(arr[j]<arr[j+1]){
				int temp=arr[j];
				arr[j]=arr[j+1];
				arr[j+1]=temp;
			}
		}
	}
}
void printArr(int arr[],int n){
	for(int i=0;i<n;i++){
		printf("%d ",arr[i]);
	}
	printf("\n");
}
int main(){
	int n;
	scanf("%d",&n);
	if(n<=0){
		printf("Invalid number\n");
		return 0;
	}
	int *arr=malloc(n*sizeof(int));
	for(int i=0;i<n;i++){
		scanf("%d",&arr[i]);
	}

	printf("Original Scores: ");
	printArr(arr,n);
	bubbleSort(arr,n);
	printf("Sorted Scores: ");
	printArr(arr,n);
	return 0;
}


```

## Compilation
```bash
gcc "1.7.5. Sort the scores of athletes in descending order.c" -o program
```

## Usage
```bash
./program
```

## Notes
- This is part of a Data Structures course (CS201B)
- Auto-generated documentation - Full detailed documentation will be generated by the CI workflow
