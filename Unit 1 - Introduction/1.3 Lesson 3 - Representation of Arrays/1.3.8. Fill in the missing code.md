# 1.3.8. Fill in the missing code
## 📋 Overview
This C program creates a 2D array (matrix) with user-specified dimensions, populates it with user-input values, displays the matrix, and calculates and displays the sum of elements in each row.

## 🎯 Purpose
The purpose of this program is to demonstrate basic matrix operations, such as input, output, and calculation of row sums. It provides a simple and interactive way to work with matrices, making it a useful tool for educational purposes or data analysis.

## ✨ Key Features
* User-specified matrix dimensions
* Input and display of matrix elements
* Calculation and display of row sums
* Interactive user interface

## 🏗️ Data Structures Used
The primary data structure used in this program is a 2D array (matrix) of integers, declared as `int arr[5][5]`. Although the array is declared with fixed dimensions (5x5), the program allows the user to specify the actual number of rows and columns to use, making it a dynamic matrix in terms of usage.

## 🔧 Implementation Details

### Algorithm Explanation
The program uses the following steps to achieve its functionality:
1. **Get row and column sizes**: The program prompts the user to enter the row and column sizes of the matrix using `printf()` and `scanf()`.
2. **Initialize matrix**: The program uses nested loops to iterate over the elements of the matrix, prompting the user to enter values for each element using `printf()` and `scanf()`.
3. **Display matrix**: The program uses nested loops to iterate over the elements of the matrix, displaying each element using `printf()`.
4. **Calculate row sums**: The program uses nested loops to iterate over the elements of the matrix, calculating the sum of elements in each row using a temporary variable `sum`.
5. **Display row sums**: The program displays the sum of elements in each row using `printf()`.

### Functions Documentation
- **Function Name**: `main()`
  - **Purpose**: The entry point of the program, responsible for executing the main logic.
  - **Parameters**: None
  - **Returns**: None (void)
  - **Logic**: The `main()` function orchestrates the entire program, calling other functions and executing the algorithm.
- **Function Name**: `printf()`
  - **Purpose**: A standard library function used for printing output to the console.
  - **Parameters**: A format string and variable number of arguments.
  - **Returns**: The number of characters printed.
  - **Logic**: `printf()` is used to display messages, prompts, and results to the user.
- **Function Name**: `scanf()`
  - **Purpose**: A standard library function used for reading input from the console.
  - **Parameters**: A format string and variable number of arguments.
  - **Returns**: The number of successful assignments.
  - **Logic**: `scanf()` is used to read input values from the user.

## 💻 Compilation & Usage

### Compilation
```bash
gcc "1.3.8. Fill in the missing code.c" -o 1.3.8. Fill in the missing code
```

### Running the Program
```bash
./1.3.8. Fill in the missing code
```

## 📥 Input/Output

### Input
The program takes the following inputs:
* **Row size**: An integer value specifying the number of rows in the matrix.
* **Column size**: An integer value specifying the number of columns in the matrix.
* **Matrix elements**: Integer values populating the matrix, entered by the user.

### Output
The program produces the following outputs:
* **Matrix display**: The program displays the populated matrix.
* **Row sum display**: The program displays the sum of elements in each row.

## 📊 Complexity Analysis
- **Time Complexity**: The time complexity of the program is O(rows \* cols) + O(rows \* cols) + O(rows \* cols) = O(3 \* rows \* cols), which simplifies to O(rows \* cols). This is because the program performs three main operations: input, display, and calculation of row sums, each of which involves iterating over the elements of the matrix.
- **Space Complexity**: The space complexity of the program is O(1), as the program uses a fixed amount of memory to store the matrix and temporary variables, regardless of the input size.

## 🧪 Example Run
```
Enter row and column sizes : 3 3
Enter the value of arr[0][0] : 1
Enter the value of arr[0][1] : 2
Enter the value of arr[0][2] : 3
Enter the value of arr[1][0] : 4
Enter the value of arr[1][1] : 5
Enter the value of arr[1][2] : 6
Enter the value of arr[2][0] : 7
Enter the value of arr[2][1] : 8
Enter the value of arr[2][2] : 9
The given matrix is
1 2 3 
4 5 6 
7 8 9 
Sum of row - 0 elements = 6
Sum of row - 1 elements = 15
Sum of row - 2 elements = 24
```

## ⚠️ Edge Cases & Error Handling
The program does not handle the following edge cases:
* **Invalid input**: The program does not check for invalid input values, such as non-integer values or values outside the range of the matrix.
* **Matrix size limits**: The program does not check if the user-specified matrix size exceeds the declared size of the array (5x5).
* **Division by zero**: The program does not perform any division operations, so this edge case is not applicable.

## 🚧 Limitations
The program has the following potential issues or limitations:
* **Fixed array size**: The program declares a fixed-size array (5x5), which may not be sufficient for larger matrices.
* **No error handling**: The program does not handle invalid input values or other errors, which may cause the program to crash or produce incorrect results.
* **No validation**: The program does not validate the user-specified matrix size or input values, which may lead to incorrect results or crashes.

## 📝 Notes
This program is a basic implementation of matrix operations and is intended for educational purposes. It can be extended or modified to handle more complex matrix operations or to improve its robustness and error handling.