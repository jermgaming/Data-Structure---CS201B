# 1.6.1. Array Sum of Positives and Negatives
## 📋 Overview
The Array Sum of Positives and Negatives program is a C-based application designed to calculate and display the sum of all positive numbers and the sum of all negative numbers in an array of integers. The array is dynamically allocated based on user input, and the program reads the array elements from standard input.

## 🎯 Purpose
The purpose of this program is to provide a simple and efficient solution for calculating the sum of positive and negative numbers in an array. This can be useful in various scenarios, such as data analysis, statistical calculations, or educational purposes. The program aims to demonstrate the use of dynamic memory allocation, array operations, and basic algorithms in C programming.

## ✨ Key Features
* Dynamically allocated array of integers based on user input
* Calculation of sum of all positive numbers in the array
* Calculation of sum of all negative numbers in the array
* Input validation for array size and elements
* Error handling for memory allocation failures

## 🏗️ Data Structures Used
The program uses the following data structures:
* **Arrays**: A dynamically allocated array of integers (`int *arr`) is used to store the input numbers. The array is defined as a pointer to an integer, and its size is determined by the user input.
* **Pointers**: A pointer to an integer (`int *arr`) is used to dynamically allocate memory for the array.
* **Variables**: Integer variables (`int n`, `int ans`, `int positive`, `int negative`) are used to store the array size, sum of positive numbers, sum of negative numbers, and other temporary values.

## 🔧 Implementation Details

### Algorithm Explanation
The program uses a linear search approach to iterate through the array and calculate the sum of positive and negative numbers. The algorithm can be broken down into the following steps:
1. Read the array size (`int n`) from standard input using `scanf`.
2. Dynamically allocate memory for the array using `malloc` based on the user-input size.
3. Read the array elements from standard input using `scanf` and store them in the dynamically allocated array.
4. Call the `posSum` function to calculate the sum of all positive numbers in the array.
5. Call the `negSum` function to calculate the sum of all negative numbers in the array.
6. Print the sum of all positive numbers and the sum of all negative numbers to standard output using `printf`.

### Functions Documentation
- **`posSum(int arr[], int n)`**:
  - **Purpose**: Calculate the sum of all positive numbers in the array.
  - **Parameters**:
    - `arr`: The input array of integers.
    - `n`: The size of the input array.
  - **Returns**: The sum of all positive numbers in the array.
  - **Logic**: The function iterates through the array using a linear search approach and adds up all the positive numbers.
- **`negSum(int arr[], int n)`**:
  - **Purpose**: Calculate the sum of all negative numbers in the array.
  - **Parameters**:
    - `arr`: The input array of integers.
    - `n`: The size of the input array.
  - **Returns**: The sum of all negative numbers in the array.
  - **Logic**: The function iterates through the array using a linear search approach and adds up all the negative numbers.
- **`main()`**:
  - **Purpose**: The entry point of the program, responsible for reading input, allocating memory, calling the `posSum` and `negSum` functions, and printing the results.
  - **Parameters**: None.
  - **Returns**: An integer indicating the program's exit status (0 for success).
  - **Logic**: The function reads the array size and elements from standard input, allocates memory for the array, calls the `posSum` and `negSum` functions, and prints the results.

## 💻 Compilation & Usage

### Compilation
```bash
gcc "1.6.1. Array Sum of Positives and Negatives.c" -o 1.6.1. Array Sum of Positives and Negatives
```

### Running the Program
```bash
./1.6.1. Array Sum of Positives and Negatives
```

## 📥 Input/Output

### Input
The program expects the following inputs:
* The size of the array (`int n`): Read from standard input using `scanf`.
* The array elements (`int arr[]`): Read from standard input using `scanf`.

### Output
The program produces the following outputs:
* The sum of all positive numbers in the array (`int positive`): Printed to standard output using `printf`.
* The sum of all negative numbers in the array (`int negative`): Printed to standard output using `printf`.

## 📊 Complexity Analysis
- **Time Complexity**: The program has a time complexity of O(n), where n is the size of the input array. This is because the `posSum` and `negSum` functions iterate through the array once, and the `main` function calls these functions.
- **Space Complexity**: The program has a space complexity of O(n), where n is the size of the input array. This is because the program dynamically allocates memory for the array.

## 🧪 Example Run
```
Enter the size of the array: 5
Enter the array elements: 1 2 3 -4 -5
Sum of positive numbers: 6
Sum of negative numbers: -9
```

## ⚠️ Edge Cases & Error Handling
The program does not handle the following edge cases:
* **Invalid Input**: The program does not check for invalid input, such as non-integer values or negative array sizes.
* **Memory Allocation Failure**: The program does not check for memory allocation failures, which can occur if the system runs out of memory.
* **Division by Zero**: The program does not perform any division operations, so division by zero is not a concern.

## 🚧 Limitations
The program has the following limitations:
* **Integer Overflow**: The program uses integer variables to store the sum of positive and negative numbers, which can lead to integer overflow for large input arrays.
* **Memory Leaks**: The program does not free the dynamically allocated memory, which can lead to memory leaks if the program is run multiple times.
* **Input Validation**: The program does not validate the input array size or elements, which can lead to incorrect results or crashes if the input is invalid.

## 📝 Notes
The program is designed to demonstrate the use of dynamic memory allocation, array operations, and basic algorithms in C programming. It is not intended for production use and should be modified and expanded to handle edge cases and limitations.