# 1.5.11. Merge Sort
## 📋 Overview
The Merge Sort program is a C implementation of the popular Merge Sort algorithm, a divide-and-conquer technique used to sort arrays of integers in ascending order. This program takes an array of integers as input, splits it into smaller sub-arrays, merges and sorts them recursively, and finally displays the sorted array.

## 🎯 Purpose
The purpose of this program is to demonstrate the effectiveness of the Merge Sort algorithm in sorting large datasets efficiently. Merge Sort is a stable sorting algorithm that is widely used in many applications due to its simplicity, efficiency, and scalability. This program exists to provide a clear and concise implementation of the Merge Sort algorithm, making it easier for developers to understand and integrate into their own projects.

## ✨ Key Features
* Implements the Merge Sort algorithm to sort arrays of integers in ascending order
* Uses a recursive approach to split the input array into smaller sub-arrays
* Merges and sorts the sub-arrays using a temporary array
* Displays the sorted array as output
* Handles arrays of up to 15 elements

## 🏗️ Data Structures Used
The program uses the following data structures:
* **Arrays**: One-dimensional arrays are used to store integers. The `arr` array is used to store the input array, while the `left` and `right` arrays are used to store the left and right sub-arrays during the merge process.
* **Variables**: Integer variables are used to store indices, lengths, and temporary values.

## 🔧 Implementation Details

### Algorithm Explanation
The Merge Sort algorithm works by recursively splitting the input array into smaller sub-arrays until each sub-array contains only one element. The sub-arrays are then merged and sorted using a temporary array. The process is repeated until the entire array is sorted.

1. The `splitAndMerge` function is called with the input array, starting index (`low`), and ending index (`high`).
2. If the length of the sub-array is greater than 1 (`low < high`), the function recursively splits the sub-array into two smaller sub-arrays by calculating the midpoint (`mid`).
3. The `splitAndMerge` function is called recursively for the left sub-array (`low` to `mid`) and the right sub-array (`mid + 1` to `high`).
4. Once the recursive calls return, the `merge` function is called to merge the two sorted sub-arrays into a single sorted sub-array.
5. The `merge` function creates two temporary arrays (`left` and `right`) to store the elements of the left and right sub-arrays.
6. The `merge` function iterates through the `left` and `right` arrays, comparing elements and placing the smaller element in the original array.
7. The `merge` function continues until all elements from both sub-arrays have been merged into the original array.
8. The `splitAndMerge` function returns, and the process is repeated for the next level of recursion.
9. Once the recursion is complete, the sorted array is displayed using the `display` function.

### Functions Documentation
- **`display(int arr[15], int n)`**:
  - **Purpose**: Displays the elements of the input array.
  - **Parameters**: `arr` (the input array), `n` (the number of elements in the array).
  - **Returns**: None (void).
  - **Logic**: Iterates through the input array and prints each element.
- **`merge(int arr[15], int low, int mid, int high)`**:
  - **Purpose**: Merges two sorted sub-arrays into a single sorted sub-array.
  - **Parameters**: `arr` (the input array), `low` (the starting index of the left sub-array), `mid` (the ending index of the left sub-array), `high` (the ending index of the right sub-array).
  - **Returns**: None (void).
  - **Logic**: Creates temporary arrays to store the left and right sub-arrays, iterates through the sub-arrays, and merges them into the original array.
- **`splitAndMerge(int arr[15], int low, int high)`**:
  - **Purpose**: Recursively splits the input array into smaller sub-arrays and merges them using the `merge` function.
  - **Parameters**: `arr` (the input array), `low` (the starting index of the sub-array), `high` (the ending index of the sub-array).
  - **Returns**: None (void).
  - **Logic**: Recursively splits the sub-array into smaller sub-arrays and calls the `merge` function to merge and sort them.

## 💻 Compilation & Usage

### Compilation
```bash
gcc "1.5.11. Merge Sort.c" -o 1.5.11. Merge Sort
```

### Running the Program
```bash
./1.5.11. Merge Sort
```

## 📥 Input/Output

### Input
The program expects an array of integers as input. The array should be a one-dimensional array of integers with a maximum size of 15 elements.

### Output
The program produces the sorted array as output. The output is displayed in the console, with each element separated by a space.

## 📊 Complexity Analysis
- **Time Complexity**: The time complexity of the Merge Sort algorithm is **O(n log n)**, where **n** is the number of elements in the input array. This is because the algorithm divides the array into two halves at each level of recursion, resulting in a logarithmic number of levels. At each level, the algorithm merges the sub-arrays, resulting in a linear number of comparisons.
- **Space Complexity**: The space complexity of the Merge Sort algorithm is **O(n)**, where **n** is the number of elements in the input array. This is because the algorithm uses temporary arrays to store the elements of the sub-arrays during the merge process.

## 🧪 Example Run
```
Input: [3, 6, 1, 8, 2, 4]
Output: [1, 2, 3, 4, 6, 8]
```

## ⚠️ Edge Cases & Error Handling
The program does not explicitly handle edge cases or errors. However, some potential edge cases to consider are:
* **Empty array**: If the input array is empty, the program will not crash, but it will not display any output.
* **Single-element array**: If the input array contains only one element, the program will not crash, but it will not perform any sorting.
* **Duplicate elements**: If the input array contains duplicate elements, the program will sort them correctly.
* **Negative numbers**: If the input array contains negative numbers, the program will sort them correctly.

## 🚧 Limitations
Some potential issues or limitations of the program are:
* **Fixed array size**: The program assumes a fixed array size of 15 elements. If the input array is larger, the program will not work correctly.
* **Integer overflow**: If the input array contains very large integers, the program may experience integer overflow during the sorting process.
* **Lack of error handling**: The program does not explicitly handle errors, such as invalid input or memory allocation failures.

## 📝 Notes
The Merge Sort algorithm is a stable sorting algorithm that is widely used in many applications due to its simplicity, efficiency, and scalability. This program provides a clear and concise implementation of the Merge Sort algorithm, making it easier for developers to understand and integrate into their own projects.