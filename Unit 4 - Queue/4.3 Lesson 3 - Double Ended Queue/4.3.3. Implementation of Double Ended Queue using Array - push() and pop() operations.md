# 4.3.3. Implementation of Double Ended Queue using Array - push() and pop() operations

## Description
This C program is located at `Unit 4 - Queue/4.3 Lesson 3 - Double Ended Queue/4.3.3. Implementation of Double Ended Queue using Array - push() and pop() operations.c`.

## Code
```c
#define MAX 20
int deQueue	[MAX];
int front = -1, rear = -1;

void push(int x) {
	if(front==-1 || front==0){
		printf("Double ended queue is overflow.\n");
		return;
	}
	front--;
	deQueue[front]=x;
	printf("Successfully inserted at front side.\n");
	return;
}

void pop() {
	if(front==-1){
		printf("Double ended queue is underflow.\n");
		return;
	}
	printf("Deleted element from front side = %d\n",deQueue[front]);
	if(front==rear){
		front=-1;
		rear=-1;
		return;
	}
	front++;
	
	
}

void eject()...
```

## Compilation
```bash
gcc "4.3.3. Implementation of Double Ended Queue using Array - push() and pop() operations.c" -o 4.3.3. Implementation of Double Ended Queue using Array - push() and pop() operations
```

## Usage
```bash
./4.3.3. Implementation of Double Ended Queue using Array - push() and pop() operations
```

## Notes
- Auto-generated README (API key not configured for detailed analysis)
- Please add detailed documentation manually
