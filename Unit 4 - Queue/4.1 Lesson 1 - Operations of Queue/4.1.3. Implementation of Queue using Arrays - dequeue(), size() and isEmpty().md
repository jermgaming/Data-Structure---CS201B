# 4.1.3. Implementation of Queue using Arrays - dequeue(), size() and isEmpty()

## Description
This C program is located at `Unit 4 - Queue/4.1 Lesson 1 - Operations of Queue/4.1.3. Implementation of Queue using Arrays - dequeue(), size() and isEmpty().c`.

## Code
```c
#define MAX 10
int queue[MAX];
int front = -1, rear = -1;

void dequeue() {
	if(front==-1 || front>rear){
		printf("Queue is underflow.\n");
	}else {
		printf("Deleted element = %d\n",queue[front++]);
	}

	if(front>rear){
		front=-1;
		rear=-1;
	}
}

void size() {
	if(front==-1 || front>rear){
		printf("Queue size : %d\n",0);
	}else{
		printf("Queue size : %d\n",rear-front+1);
	}
}

void isEmpty() {
	if(front==-1 || front>rear){
		printf("Queue is empty.\n");
	}else {
		printf("Queue is not empt...
```

## Compilation
```bash
gcc "4.1.3. Implementation of Queue using Arrays - dequeue(), size() and isEmpty().c" -o 4.1.3. Implementation of Queue using Arrays - dequeue(), size() and isEmpty()
```

## Usage
```bash
./4.1.3. Implementation of Queue using Arrays - dequeue(), size() and isEmpty()
```

## Notes
- Auto-generated README (API key not configured for detailed analysis)
- Please add detailed documentation manually
