# 2.7.2. Linked List Intersection at a Specified Position
## 📋 Overview
This C program creates two linked lists, intersects them at a specified position, and then prints the resulting linked lists. The intersection point is determined by the user, who provides the position in the first linked list where the second linked list should be attached.

## 🎯 Purpose
The purpose of this program is to demonstrate the implementation of linked lists and their intersection at a specified position. It solves the problem of combining two linked lists at a particular point, which is a fundamental operation in data structures and algorithms. This program can be used as a building block for more complex applications, such as database query optimization or graph algorithms.

## ✨ Key Features
* Creates two linked lists from user input
* Intersects the two linked lists at a specified position
* Prints the resulting linked lists
* Uses a singly linked list data structure
* Implements functions for creating nodes, inserting nodes at the end of the list, and printing the list

## 🏗️ Data Structures Used
The program uses a singly linked list data structure, which is implemented using a `struct node` with two members: `data` (an integer) and `next` (a pointer to the next node in the list). The linked list is defined as a sequence of nodes, where each node points to the next node in the list. The `next` pointer of the last node is set to `NULL` to indicate the end of the list.

## 🔧 Implementation Details

### Algorithm Explanation
The program uses the following steps to intersect the two linked lists:
1. Read the number of nodes in the first linked list (`l1`) and create the linked list by reading the data for each node and inserting it at the end of the list using `insertTail`.
2. Read the number of nodes in the second linked list (`l2`) and create the linked list by reading the data for each node and inserting it at the end of the list using `insertTail`.
3. Read the position in the first linked list where the second linked list should be attached (`p`).
4. Find the node at the specified position in the first linked list by traversing the list `p` times.
5. Find the last node in the second linked list by traversing the list until the `next` pointer is `NULL`.
6. Attach the second linked list to the first linked list by setting the `next` pointer of the last node in the second linked list to the node at the specified position in the first linked list.
7. Print the data of all nodes in the first linked list using `print`.
8. Print the data of all nodes in the second linked list using `print`.

### Functions Documentation
- **Function Name**: `createNode(int data)`
  - **Purpose**: Creates a new node with the given data and returns a pointer to it.
  - **Parameters**: `data` (an integer)
  - **Returns**: A pointer to the newly created node (`struct node*`)
  - **Logic**: Allocates memory for a new node, sets its `data` member to the given value, and sets its `next` pointer to `NULL`.
- **Function Name**: `insertTail(struct node* head, int data)`
  - **Purpose**: Inserts a new node with the given data at the end of the linked list.
  - **Parameters**: `head` (a pointer to the head of the linked list), `data` (an integer)
  - **Returns**: A pointer to the head of the updated linked list (`struct node*`)
  - **Logic**: Creates a new node using `createNode`, and then traverses the list to find the last node. If the list is empty, sets the `head` pointer to the new node. Otherwise, sets the `next` pointer of the last node to the new node.
- **Function Name**: `print(struct node* head)`
  - **Purpose**: Prints the data of all nodes in the linked list.
  - **Parameters**: `head` (a pointer to the head of the linked list)
  - **Returns**: None (void)
  - **Logic**: Traverses the list, printing the `data` member of each node. If the `next` pointer is not `NULL`, prints a space after each node's data.
- **Function Name**: `main()`
  - **Purpose**: The program's entry point, responsible for reading input, creating linked lists, intersecting them, and printing the results.
  - **Parameters**: None (void)
  - **Returns**: An integer indicating the program's exit status (0 for success)
  - **Logic**: Reads input from the user, creates the linked lists using `insertTail`, intersects the lists, and prints the results using `print`.

## 💻 Compilation & Usage

### Compilation
```bash
gcc "2.7.2. Linked List Intersection at a Specified Position.c" -o 2.7.2. Linked List Intersection at a Specified Position
```

### Running the Program
```bash
./2.7.2. Linked List Intersection at a Specified Position
```

## 📥 Input/Output

### Input
The program expects the following input:
* The number of nodes in the first linked list (`l1`)
* The data for each node in the first linked list (`data`)
* The number of nodes in the second linked list (`l2`)
* The data for each node in the second linked list (`data`)
* The position in the first linked list where the second linked list should be attached (`p`)

### Output
The program produces the following output:
* The data of all nodes in the first linked list after intersection
* The data of all nodes in the second linked list after intersection

## 📊 Complexity Analysis
- **Time Complexity**: O(n + m), where n is the number of nodes in the first linked list and m is the number of nodes in the second linked list. This is because the program traverses each linked list once to create it, and then traverses the first linked list again to find the intersection point.
- **Space Complexity**: O(n + m), where n is the number of nodes in the first linked list and m is the number of nodes in the second linked list. This is because the program allocates memory for each node in both linked lists.

## 🧪 Example Run
```
Input:
3
1 2 3
3
4 5 6
1
Output:
1 2 4 5 6
4 5 6
```

## ⚠️ Edge Cases & Error Handling
The program does not handle the following edge cases:
* If the user enters a negative value for the number of nodes in either linked list, the program will not behave correctly.
* If the user enters a value for the intersection position that is greater than the number of nodes in the first linked list, the program will not behave correctly.
* If the user enters a value for the intersection position that is less than 0, the program will not behave correctly.
* If the program runs out of memory while allocating nodes, it will terminate with an error.

## 🚧 Limitations
The program has the following limitations:
* The program uses a simple iterative approach to insert nodes at the end of the linked list, which can be inefficient for large linked lists.
* The program uses a simple iterative approach to find the intersection point in the first linked list, which can be inefficient for large linked lists.
* The program does not handle edge cases or errors, which can cause it to behave incorrectly or terminate unexpectedly.
* The program uses a fixed amount of memory to store the linked lists, which can limit its ability to handle large input sizes.

## 📝 Notes
This program is intended for educational purposes and demonstrates the implementation of linked lists and their intersection at a specified position. It can be used as a building block for more complex applications, but it may require modifications to handle edge cases and improve efficiency.