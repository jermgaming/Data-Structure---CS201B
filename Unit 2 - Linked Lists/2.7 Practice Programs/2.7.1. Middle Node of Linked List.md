# 2.7.1. Middle Node of Linked List
## 📋 Overview
This C program is designed to find and print the middle node of a singly linked list. It takes a series of integers as input, constructs a linked list from these integers, and then uses the slow and fast pointer technique to find the middle node of the list.

## 🎯 Purpose
The primary purpose of this program is to demonstrate the implementation of a singly linked list and the use of the slow and fast pointer technique to find the middle node. This technique is useful in various applications, such as data structures and algorithms, where finding the middle element of a linked list is necessary.

## ✨ Key Features
*   Constructs a singly linked list from a series of input integers
*   Uses the slow and fast pointer technique to find the middle node
*   Prints the value of the middle node
*   Handles user input and provides output

## 🏗️ Data Structures Used
The program implements and uses the following data structures:

*   **Singly Linked List**: A data structure in which each node contains a value and a reference (or "link") to the next node in the sequence. This is the primary data structure used in the program.
*   **Struct node**: A custom data structure defined by the program to represent each node in the linked list. Each `struct node` contains an integer `data` field and a `next` field, which is a pointer to the next node in the list.

## 🔧 Implementation Details

### Algorithm Explanation
The program uses the slow and fast pointer technique to find the middle node of the linked list. Here's a step-by-step breakdown of the algorithm:

1.  Initialize two pointers, `slow` and `fast`, to the head of the linked list.
2.  Move the `slow` and `fast` pointers through the list:
    *   In each iteration, move the `slow` pointer one step forward (`slow = slow->next`).
    *   Move the `fast` pointer two steps forward (`fast = fast->next->next`).
3.  When the `fast` pointer reaches the end of the list, the `slow` pointer will be at the middle node.
4.  Print the value of the middle node using `printf`.

### Functions Documentation
The program defines the following functions:

*   **`createNode(int data)`**:
    *   **Purpose**: Creates a new node with the given `data` value and returns a pointer to this node.
    *   **Parameters**: `int data` - the value to be stored in the new node.
    *   **Returns**: `struct node*` - a pointer to the newly created node.
    *   **Logic**: Allocates memory for a new node, sets its `data` field to the given value, and sets its `next` field to `NULL`.
*   **`insertTail(struct node* head, int data)`**:
    *   **Purpose**: Inserts a new node with the given `data` value at the end of the linked list.
    *   **Parameters**:
        *   `struct node* head` - the head of the linked list.
        *   `int data` - the value to be stored in the new node.
    *   **Returns**: `struct node*` - the updated head of the linked list.
    *   **Logic**: Creates a new node using `createNode`, and if the list is empty, returns the new node. Otherwise, traverses the list to find the last node and appends the new node to it.
*   **`print(struct node* head)`**:
    *   **Purpose**: Prints the values of all nodes in the linked list.
    *   **Parameters**: `struct node* head` - the head of the linked list.
    *   **Returns**: None (void).
    *   **Logic**: Traverses the list, printing the `data` value of each node.
*   **`main()`**:
    *   **Purpose**: The entry point of the program, responsible for reading input, constructing the linked list, finding the middle node, and printing the result.
    *   **Parameters**: None (void).
    *   **Returns**: `int` - the program's exit status (0 for success).
    *   **Logic**: Reads the number of nodes, constructs the linked list by reading node values and inserting them at the end of the list, and then finds and prints the middle node using the slow and fast pointer technique.

## 💻 Compilation & Usage

### Compilation
```bash
gcc "2.7.1. Middle Node of Linked List.c" -o 2.7.1. Middle Node of Linked List
```

### Running the Program
```bash
./2.7.1. Middle Node of Linked List
```

## 📥 Input/Output

### Input
The program expects the following input:

*   An integer `n`, representing the number of nodes in the linked list.
*   `n` integers, which are used to construct the linked list.

### Output
The program prints the value of the middle node in the linked list.

## 📊 Complexity Analysis
*   **Time Complexity**: The time complexity of constructing the linked list is O(n), where n is the number of nodes. The time complexity of finding the middle node using the slow and fast pointer technique is O(n), where n is the number of nodes. The overall time complexity of the program is O(n) + O(n) = O(2n), which simplifies to O(n).
*   **Space Complexity**: The space complexity of the program is O(n), where n is the number of nodes in the linked list. This is because the program uses a linked list to store the input data.

## 🧪 Example Run
```
Enter the number of nodes: 5
Enter node values: 1 2 3 4 5
Middle node value: 3
```

## ⚠️ Edge Cases & Error Handling
The program does not handle the following edge cases:

*   **Empty Linked List**: If the input `n` is 0, the program will not handle this case correctly. It will not print any output, but it will not report an error either.
*   **Invalid Input**: If the input `n` is negative, the program will not handle this case correctly. It will not report an error, but it will not construct the linked list correctly either.
*   **Memory Allocation Errors**: If the program runs out of memory while constructing the linked list, it will not handle this case correctly. It will not report an error, but it will crash or produce undefined behavior.

To handle these edge cases, the program should add error checking code to handle invalid input and memory allocation errors.

## 🚧 Limitations
The program has the following potential issues or limitations:

*   **Memory Usage**: The program uses a linked list to store the input data, which can be memory-intensive for large inputs.
*   **Input Validation**: The program does not validate the input data, which can lead to errors or crashes if the input is invalid.
*   **Error Handling**: The program does not handle errors correctly, which can lead to crashes or undefined behavior if an error occurs.

To address these issues, the program should add error checking code to handle invalid input and memory allocation errors, and it should use more efficient data structures or algorithms to reduce memory usage.

## 📝 Notes
This program demonstrates the implementation of a singly linked list and the use of the slow and fast pointer technique to find the middle node. It is a useful tool for educational purposes, testing linked list implementations, and finding the middle node in various applications.