# 2.2.4. Write a Program to Insert an element at Begin and Count number of Nodes in Doubly Linked List

## Description
This program implements 2.2.4. Write a Program to Insert an element at Begin and Count number of Nodes in Doubly Linked List

## File Location
`Unit 2 - Linked Lists/2.2 Lesson 2 - Doubly Linked List/2.2.4. Write a Program to Insert an element at Begin and Count number of Nodes in Doubly Linked List.c`

## Code Preview
```c
struct node {
	int data;
	struct node *prev;
	struct node *next;
};
typedef struct node * NODE;

NODE createNodeInDLL() {
	NODE temp;
	temp = (NODE)malloc(sizeof(struct node));
	temp -> prev = NULL;
	temp -> next = NULL;
	return temp;
}

int countInDLL(NODE first) {
	int count=0;
	NODE temp=first;
	while(temp){
		count++;
		temp=temp->next;
	}
	return count;
}

NODE insertAtBeginInDLL(NODE first, int x) {
	NODE temp=createNodeInDLL();
	temp->data=x;
	temp->next=first;
	first->prev=temp;
	first=temp;
	return first;
}

void traverseListInDLL(NODE first) {
	NODE lastNode = first;
	while (lastNode != NULL) {
		printf("%d <--> ", lastNode -> data);
		lastNode = lastNode -> next;
	}
	printf("NULL\n");
}


```

## Compilation
```bash
gcc "2.2.4. Write a Program to Insert an element at Begin and Count number of Nodes in Doubly Linked List.c" -o program
```

## Usage
```bash
./program
```

## Notes
- This is part of a Data Structures course (CS201B)
- Auto-generated documentation - Full detailed documentation will be generated by the CI workflow
