# 2.2.9. Write Code for searchPosOfEleInDLL() function in DLL
## 📋 Overview
The provided C program implements a Doubly Linked List (DLL) data structure, allowing users to create nodes, insert nodes at the beginning of the list, traverse the list, and search for the position of a specific element within the list.

## 🎯 Purpose
The main purpose of this program is to demonstrate the implementation of a DLL and provide functions to manipulate and traverse the list. The program exists to solve the problem of efficiently storing and retrieving data in a linked list, where each node has two pointers, `prev` and `next`, which point to the previous and next nodes in the list, respectively.

## ✨ Key Features
* Implementation of a Doubly Linked List (DLL) data structure
* Creation of new nodes in the DLL
* Insertion of nodes at the beginning of the list
* Traversal of the list to print the data of each node
* Search for the position of a specific element within the list

## 🏗️ Data Structures Used
The program implements and uses the following data structure:
* **Doubly Linked List (DLL)**: A DLL is a type of linked list where each node has two pointers, `prev` and `next`, which point to the previous and next nodes in the list, respectively. This allows for efficient traversal and insertion of nodes at any position in the list. The DLL is defined using a `struct node` with an `int` data field and pointers to the previous and next nodes.

## 🔧 Implementation Details

### Algorithm Explanation
The program uses the following algorithms:
* **Linear Search**: The `searchPosOfEleInDLL` function uses a linear search algorithm to traverse the DLL and find the position of a specific element.
* **Insertion at the Beginning**: The `insertAtBeginInDLL` function uses a simple insertion algorithm to add a new node at the beginning of the DLL.

### Functions Documentation
* **`createNodeInDLL()`**:
  * **Purpose**: Creates a new node in the DLL.
  * **Parameters**: None
  * **Returns**: A pointer to the newly created node (`NODE` type)
  * **Logic**: Allocates memory for the new node and initializes its `prev` and `next` pointers to `NULL`.
* **`traverseListInDLL(NODE first)`**:
  * **Purpose**: Traverses the DLL and prints the data of each node.
  * **Parameters**: `first` - a pointer to the first node in the list (`NODE` type)
  * **Returns**: None
  * **Logic**: Traverses the list from the first node to the last node, printing the data of each node.
* **`insertAtBeginInDLL(NODE head, int x)`**:
  * **Purpose**: Inserts a new node with the given data at the beginning of the DLL.
  * **Parameters**:
    * `head` - a pointer to the current head of the list (`NODE` type)
    * `x` - the data to be inserted into the new node (`int` type)
  * **Returns**: A pointer to the new head of the list (`NODE` type)
  * **Logic**: Creates a new node using `createNodeInDLL`, sets its `data` field to the given value, and updates the `prev` and `next` pointers of the new node and the current head of the list.
* **`searchPosOfEleInDLL(NODE first, int element)`**:
  * **Purpose**: Searches for the position of a specific element within the DLL.
  * **Parameters**:
    * `first` - a pointer to the first node in the list (`NODE` type)
    * `element` - the element to be searched for (`int` type)
  * **Returns**: The position of the element in the list (`int` type) or 0 if the element is not found
  * **Logic**: Traverses the list from the first node to the last node, checking if the `data` field of each node matches the given element. If a match is found, the function returns the position of the element.

## 💻 Compilation & Usage

### Compilation
```bash
gcc "2.2.9. Write Code for searchPosOfEleInDLL() function in DLL.c" -o 2.2.9. Write Code for searchPosOfEleInDLL() function in DLL
```

### Running the Program
```bash
./2.2.9. Write Code for searchPosOfEleInDLL() function in DLL
```

## 📥 Input/Output

### Input
The program takes the following inputs:
* **Data for new nodes**: The `insertAtBeginInDLL` function takes an integer value as input to insert into the new node.
* **Element to search for**: The `searchPosOfEleInDLL` function takes an integer value as input to search for in the DLL.

### Output
The program produces the following outputs:
* **Traversed list**: The `traverseListInDLL` function prints the data of each node in the DLL.
* **Position of the element**: The `searchPosOfEleInDLL` function returns the position of the element in the DLL or 0 if the element is not found.

## 📊 Complexity Analysis
* **Time Complexity**:
  * `createNodeInDLL`: O(1) - constant time complexity
  * `traverseListInDLL`: O(n) - linear time complexity, where n is the number of nodes in the list
  * `insertAtBeginInDLL`: O(1) - constant time complexity
  * `searchPosOfEleInDLL`: O(n) - linear time complexity, where n is the number of nodes in the list
* **Space Complexity**:
  * The program uses a dynamic memory allocation approach to create new nodes in the DLL. The space complexity is O(n), where n is the number of nodes in the list.

## 🧪 Example Run
```c
int main() {
    NODE head = NULL;
    head = insertAtBeginInDLL(head, 10);
    head = insertAtBeginInDLL(head, 20);
    head = insertAtBeginInDLL(head, 30);
    traverseListInDLL(head);
    int pos = searchPosOfEleInDLL(head, 20);
    printf("Position of 20: %d\n", pos);
    return 0;
}
```
This example code creates a DLL with three nodes, traverses the list, and searches for the position of the element 20. The output will be:
```
30 <--> 20 <--> 10 <--> NULL
Position of 20: 2
```

## ⚠️ Edge Cases & Error Handling
The program does not handle the following edge cases:
* **NULL pointer**: The program does not check for NULL pointers when traversing the list or inserting new nodes. If a NULL pointer is encountered, the program may crash or produce unexpected results.
* **Empty list**: The program does not handle the case where the list is empty. If the list is empty, the `traverseListInDLL` function will not print any data, and the `searchPosOfEleInDLL` function will return 0.
* **Duplicate elements**: The program does not handle the case where duplicate elements are inserted into the list. If duplicate elements are inserted, the `searchPosOfEleInDLL` function will return the position of the first occurrence of the element.

## 🚧 Limitations
The program has the following potential issues or limitations:
* **Memory leaks**: The program uses dynamic memory allocation to create new nodes in the DLL. If the nodes are not properly deallocated, memory leaks may occur.
* **Inefficient search**: The `searchPosOfEleInDLL` function uses a linear search algorithm, which may be inefficient for large lists. A more efficient search algorithm, such as a binary search, may be needed for large lists.
* **Limited functionality**: The program only provides basic functionality for creating and traversing a DLL. Additional functionality, such as deleting nodes or inserting nodes at specific positions, may be needed for more complex applications.

## 📝 Notes
This program demonstrates the implementation of a Doubly Linked List (DLL) and provides functions to manipulate and traverse the list. The program uses a dynamic memory allocation approach to create new nodes in the DLL and has a time complexity of O(n) for traversing and searching the list. However, the program does not handle edge cases such as NULL pointers, empty lists, or duplicate elements, and has limitations such as memory leaks and inefficient search algorithms.