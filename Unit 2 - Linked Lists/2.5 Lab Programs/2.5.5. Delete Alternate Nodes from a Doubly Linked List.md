# 2.5.5. Delete Alternate Nodes from a Doubly Linked List
## 📋 Overview
This C program creates a doubly linked list, inserts nodes at the tail of the list, prints the list, deletes alternate nodes from the list, and then prints the modified list. The program demonstrates the implementation of a doubly linked list and the deletion of alternate nodes.

## 🎯 Purpose
The purpose of this program is to provide a clear understanding of how doubly linked lists work and how to perform operations on them, such as insertion and deletion of nodes. The program solves the problem of deleting alternate nodes from a doubly linked list, which is a common operation in computer science.

## ✨ Key Features
* Creates a doubly linked list
* Inserts nodes at the tail of the list
* Prints the original list
* Deletes alternate nodes from the list
* Prints the modified list

## 🏗️ Data Structures Used
The program uses a doubly linked list data structure, which consists of nodes with two pointers: `prev` and `next`. Each node has an integer data field and pointers to the previous and next nodes in the list. The doubly linked list is defined as follows:
```c
struct node {
    struct node* prev;
    struct node* next;
    int data;
};
```
The `prev` pointer points to the previous node in the list, and the `next` pointer points to the next node in the list. This allows for efficient insertion and deletion of nodes at any position in the list.

## 🔧 Implementation Details

### Algorithm Explanation
The program uses a simple insertion algorithm to add new nodes at the tail of the doubly linked list. The deletion algorithm is a custom implementation that iterates through the list, deleting every other node. The program uses a traversal algorithm to print the elements of the doubly linked list.

Here is a step-by-step breakdown of the main algorithm:

1. Read the number of nodes to be inserted in the doubly linked list (`n`).
2. Create an empty doubly linked list (`head = NULL`).
3. Read the data for each node and insert it at the tail of the doubly linked list using the `insertTail` function.
4. Print the original doubly linked list using the `print` function.
5. Initialize a pointer (`temp`) to the second node in the list (`head->next`).
6. Enter a loop that continues until the end of the list is reached (`temp` becomes `NULL`).
7. Inside the loop:
	* Store the previous node (`p = temp->prev`).
	* Store the next node (`n = temp->next`).
	* Update the `next` pointer of the previous node to skip the current node (`p->next = n`).
	* Update the `prev` pointer of the next node to point to the previous node (`n->prev = p`).
	* Free the memory allocated for the current node (`free(temp)`).
	* Move to the next node (`temp = n->next`).
8. Print the modified doubly linked list using the `print` function.

### Functions Documentation
- **Function Name**: `createNode(int data)`
  - **Purpose**: Creates a new node with the given data and returns a pointer to the new node.
  - **Parameters**: `int data` - the data to be stored in the new node.
  - **Returns**: `struct node*` - a pointer to the new node.
  - **Logic**: Allocates memory for a new node, initializes the `data` field, and sets the `prev` and `next` pointers to `NULL`.
- **Function Name**: `insertTail(struct node* head, int data)`
  - **Purpose**: Inserts a new node with the given data at the tail of the doubly linked list.
  - **Parameters**: `struct node* head` - the head of the doubly linked list, `int data` - the data to be stored in the new node.
  - **Returns**: `struct node*` - the head of the modified doubly linked list.
  - **Logic**: Creates a new node using the `createNode` function and appends it to the end of the list.
- **Function Name**: `print(struct node* head)`
  - **Purpose**: Prints the elements of the doubly linked list.
  - **Parameters**: `struct node* head` - the head of the doubly linked list.
  - **Returns**: None (void).
  - **Logic**: Iterates through the list, printing the data of each node.

## 💻 Compilation & Usage

### Compilation
```bash
gcc "2.5.5. Delete Alternate Nodes from a Doubly Linked List.c" -o 2.5.5. Delete Alternate Nodes from a Doubly Linked List
```

### Running the Program
```bash
./2.5.5. Delete Alternate Nodes from a Doubly Linked List
```

## 📥 Input/Output

### Input
The program takes two inputs:
* The number of nodes to be inserted in the doubly linked list (`n`).
* The data to be stored in each node (`data`).

### Output
The program produces two outputs:
* The original doubly linked list.
* The modified doubly linked list after deleting alternate nodes.

## 📊 Complexity Analysis
- **Time Complexity**: The time complexity of the program is O(n), where n is the number of nodes in the list. This is because the program iterates through the list to insert nodes, print the list, and delete alternate nodes.
- **Space Complexity**: The space complexity of the program is O(n), where n is the number of nodes in the list. This is because the program uses a doubly linked list to store the nodes, which requires O(n) space.

## 🧪 Example Run
```
Enter the number of nodes: 5
Enter the data for each node: 1 2 3 4 5
Original List: 1 <-> 2 <-> 3 <-> 4 <-> 5 <-> NULL
Modified List: 1 <-> 3 <-> 5 <-> NULL
```

## ⚠️ Edge Cases & Error Handling
The program does not handle the following edge cases:
* If the input `n` is 0, the program will not create any nodes and will print an empty list.
* If the input `n` is negative, the program will not create any nodes and will print an empty list.
* If the input data is invalid (e.g., non-integer values), the program may produce incorrect results or crash.
* If the memory allocation for the nodes fails, the program will crash.

## 🚧 Limitations
The program has the following limitations:
* The program uses a simple insertion algorithm, which may not be efficient for large lists.
* The program uses a custom deletion algorithm, which may not be optimal for deleting alternate nodes.
* The program does not handle edge cases or errors, which may lead to incorrect results or crashes.

## 📝 Notes
This program provides a basic implementation of a doubly linked list and demonstrates how to perform operations on it. However, it is not intended for production use and should be modified and extended to handle edge cases and errors.