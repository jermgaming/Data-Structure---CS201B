# 2.3.8. Write Code for deleteAtPositionInCLL() function in CLL
## 📋 Overview
This C program implements a Circularly Linked List (CLL) data structure, providing functions to create nodes, insert elements at the end of the list, delete elements at a specified position, and traverse the list to display its contents. The primary focus of this program is to demonstrate the functionality of the `deleteAtPositionInCLL()` function, which removes an element from the CLL at a specified position.

## 🎯 Purpose
The purpose of this program is to showcase the implementation and usage of a CLL, specifically highlighting the `deleteAtPositionInCLL()` function. This function is essential in managing the CLL, as it allows for the removal of elements at specific positions, making it a crucial operation in various applications such as data storage, queue management, and graph algorithms.

## ✨ Key Features
* Implementation of a Circularly Linked List (CLL) data structure
* Creation of new nodes using the `createNodeInCLL()` function
* Insertion of elements at the end of the CLL using the `insertAtEndInCLL()` function
* Deletion of elements at a specified position using the `deleteAtPositionInCLL()` function
* Traversal of the CLL to display its contents using the `traverseListInCLL()` function

## 🏗️ Data Structures Used
The primary data structure used in this program is a Circularly Linked List (CLL). A CLL is a type of linked list where the last node points back to the first node, forming a circular structure. Each node in the CLL contains an integer value and a pointer to the next node in the list.

## 🔧 Implementation Details

### Algorithm Explanation
The `deleteAtPositionInCLL()` function uses a simple deletion algorithm to remove an element from the CLL at a specified position. The algorithm works as follows:
1. Check if the CLL is empty. If it is, print an error message and return NULL.
2. Check if the CLL has only one node. If it does, delete the node and return NULL.
3. Traverse the CLL to find the node at the specified position.
4. Remove the node at the specified position by updating the `next` pointer of the previous node.
5. Return the updated first node of the CLL.

### Functions Documentation
- **`createNodeInCLL()`**: Creates a new node in the CLL.
  - **Purpose**: Creates a new node with a `NULL` `next` pointer.
  - **Parameters**: None
  - **Returns**: A pointer to the newly created node
  - **Logic**: Allocates memory for a new node, sets its `next` pointer to `NULL`, and returns the node.
- **`insertAtEndInCLL(NODE first, int x)`**: Inserts a new element at the end of the CLL.
  - **Purpose**: Inserts a new element at the end of the CLL.
  - **Parameters**:
    - `first`: A pointer to the first node in the CLL
    - `x`: The value to be inserted
  - **Returns**: A pointer to the new first node in the CLL
  - **Logic**: Creates a new node with the given value, updates the `next` pointer of the last node to point to the new node, and returns the new first node.
- **`deleteAtPositionInCLL(NODE first, int pos)`**: Deletes an element at a specified position in the CLL.
  - **Purpose**: Deletes an element at a specified position in the CLL.
  - **Parameters**:
    - `first`: A pointer to the first node in the CLL
    - `pos`: The position of the element to be deleted
  - **Returns**: A pointer to the updated first node in the CLL
  - **Logic**: Traverses the CLL to find the node at the specified position, removes the node, and returns the updated first node.
- **`traverseListInCLL(NODE first)`**: Traverses the CLL and displays its contents.
  - **Purpose**: Displays the contents of the CLL.
  - **Parameters**:
    - `first`: A pointer to the first node in the CLL
  - **Returns**: None
  - **Logic**: Traverses the CLL, printing the value of each node, until the first node is reached again.

## 💻 Compilation & Usage

### Compilation
```bash
gcc "circularly_linked_list.c" -o circularly_linked_list
```

### Running the Program
```bash
./circularly_linked_list
```

## 📥 Input/Output

### Input
The program takes two types of input:
* `x`: The value to be inserted at the end of the CLL
* `pos`: The position of the element to be deleted

### Output
The program produces the following outputs:
* The updated CLL after insertion or deletion
* The deleted element (if deletion is successful)
* An error message if the CLL is empty or the specified position is invalid

## 📊 Complexity Analysis
- **Time Complexity**:
  - `createNodeInCLL()`: O(1)
  - `insertAtEndInCLL()`: O(1)
  - `deleteAtPositionInCLL()`: O(n), where n is the number of nodes in the CLL
  - `traverseListInCLL()`: O(n), where n is the number of nodes in the CLL
- **Space Complexity**:
  - `createNodeInCLL()`: O(1)
  - `insertAtEndInCLL()`: O(1)
  - `deleteAtPositionInCLL()`: O(1)
  - `traverseListInCLL()`: O(1)

## 🧪 Example Run
```
Inserting 10 at the end of the CLL: 10
Inserting 20 at the end of the CLL: 10 --> 20
Inserting 30 at the end of the CLL: 10 --> 20 --> 30
Deleting element at position 2: 10 --> 30
```

## ⚠️ Edge Cases & Error Handling
The program handles the following edge cases:
* Empty CLL: The program checks if the CLL is empty before attempting to delete an element.
* Invalid position: The program checks if the specified position is valid before attempting to delete an element.
* Single-node CLL: The program handles the case where the CLL has only one node.

## 🚧 Limitations
The program has the following limitations:
* Memory leaks: The program uses dynamic memory allocation, which can lead to memory leaks if not managed properly.
* Infinite loops: The program uses a while loop to traverse the CLL, which can lead to infinite loops if the CLL is not properly terminated.
* Data corruption: The program modifies the CLL by inserting and deleting nodes, which can lead to data corruption if not done properly.

## 📝 Notes
This program demonstrates the implementation and usage of a Circularly Linked List (CLL) data structure, focusing on the `deleteAtPositionInCLL()` function. The program provides a basic understanding of CLL operations and can be used as a starting point for more complex applications.