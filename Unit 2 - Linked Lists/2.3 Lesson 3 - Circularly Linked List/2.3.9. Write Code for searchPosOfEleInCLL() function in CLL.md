# 2.3.9. Write Code for searchPosOfEleInCLL() function in CLL
## 📋 Overview
This C program implements a Circularly Linked List (CLL) data structure and provides functions to manipulate and traverse the list. The main purpose of this program is to create a CLL, insert elements at the beginning, search for the position of a specific element, and traverse the list to display its elements.

## 🎯 Purpose
The purpose of this program is to demonstrate the implementation of a CLL and its associated functions. The CLL is a type of linked list where the last node points back to the first node, forming a circular structure. This program solves the problem of creating and manipulating a CLL, allowing users to insert elements, search for specific elements, and traverse the list.

## ✨ Key Features
*   Creates a Circularly Linked List (CLL) data structure
*   Inserts elements at the beginning of the CLL
*   Searches for the position of a specific element in the CLL
*   Traverses the CLL to display its elements
*   Uses a simple insertion algorithm and a linear search algorithm

## 🏗️ Data Structures Used
The program uses a Circularly Linked List (CLL) data structure, which is defined as follows:
```c
struct node {
    int data;
    struct node *next;
};
typedef struct node *NODE;
```
The CLL is composed of nodes, each containing an integer data field and a pointer to the next node in the list. The last node in the list points back to the first node, forming a circular structure.

## 🔧 Implementation Details

### Algorithm Explanation
The program uses the following algorithms:

*   Insertion algorithm: The `insertAtBeginInCLL` function uses a simple insertion algorithm to add a new element at the beginning of the CLL.
*   Search algorithm: The `searchPosOfEleInCLL` function uses a linear search algorithm to find the position of a specific element in the CLL.
*   Traversal algorithm: The `traverseListInCLL` function uses a simple traversal algorithm to display the elements of the CLL.

### Functions Documentation
The program consists of the following functions:

*   **`createNode()`**: Creates a new node for the CLL.
    *   **Purpose**: Creates a new node with a null next pointer.
    *   **Parameters**: None
    *   **Returns**: A pointer to the newly created node (NODE)
    *   **Logic**: Allocates memory for a new node and initializes its next pointer to null.
*   **`insertAtBeginInCLL(NODE first, int x)`**: Inserts a new element at the beginning of the CLL.
    *   **Purpose**: Inserts a new element at the beginning of the CLL.
    *   **Parameters**:
        *   `first`: A pointer to the first node of the CLL (NODE)
        *   `x`: The integer value to be inserted (int)
    *   **Returns**: A pointer to the updated first node of the CLL (NODE)
    *   **Logic**: Creates a new node with the given value, updates the next pointer of the new node to point to the current first node, and updates the next pointer of the current first node to point to the new node.
*   **`searchPosOfEleInCLL(NODE first, int key)`**: Searches for the position of a specific element in the CLL.
    *   **Purpose**: Finds the position of a specific element in the CLL.
    *   **Parameters**:
        *   `first`: A pointer to the first node of the CLL (NODE)
        *   `key`: The integer value to be searched (int)
    *   **Returns**: The position of the element in the CLL (int), or 0 if the element is not found
    *   **Logic**: Traverses the CLL using a do-while loop until the temporary node pointer reaches the first node again, checking if the data field of the current node matches the searched key.
*   **`traverseListInCLL(NODE first)`**: Traverses the CLL and displays its elements.
    *   **Purpose**: Displays the elements of the CLL.
    *   **Parameters**:
        *   `first`: A pointer to the first node of the CLL (NODE)
    *   **Returns**: None
    *   **Logic**: Traverses the CLL using a do-while loop until the temporary node pointer reaches the first node again, printing the data field of each node during traversal.

## 💻 Compilation & Usage

### Compilation
To compile the program, use the following command:
```bash
gcc "2.3.9. Write Code for searchPosOfEleInCLL() function in CLL.c" -o 2.3.9. Write Code for searchPosOfEleInCLL() function in CLL
```
### Running the Program
To run the program, use the following command:
```bash
./2.3.9. Write Code for searchPosOfEleInCLL() function in CLL
```

## 📥 Input/Output

### Input
The program takes the following inputs:

*   Integer values to be inserted into the CLL
*   The key value to be searched in the CLL

### Output
The program produces the following outputs:

*   The updated CLL after insertion
*   The position of the searched element in the CLL (or 0 if not found)
*   The elements of the CLL during traversal

## 📊 Complexity Analysis
The time complexity of the program is as follows:

*   `createNode()`: O(1)
*   `insertAtBeginInCLL()`: O(1)
*   `searchPosOfEleInCLL()`: O(n), where n is the number of nodes in the CLL
*   `traverseListInCLL()`: O(n), where n is the number of nodes in the CLL

The space complexity of the program is as follows:

*   The CLL itself requires O(n) space, where n is the number of nodes in the CLL
*   The temporary node pointers and position counters used in the functions require O(1) space

## 🧪 Example Run
Here is an example run of the program:
```c
NODE first = NULL;
first = insertAtBeginInCLL(first, 10);
first = insertAtBeginInCLL(first, 20);
first = insertAtBeginInCLL(first, 30);

traverseListInCLL(first);  // Output: 30 --> 20 --> 10 -->

int pos = searchPosOfEleInCLL(first, 20);
if (pos != 0) {
    printf("Element found at position %d\n", pos);
} else {
    printf("Element not found\n");
}
```
Output:
```
30 --> 20 --> 10 -->
Element found at position 2
```

## ⚠️ Edge Cases & Error Handling
The program does not handle the following edge cases:

*   Inserting a duplicate element into the CLL
*   Searching for an element that is not in the CLL
*   Traversing an empty CLL

The program also does not perform any error checking on the inputs, such as checking for null pointers or invalid integer values.

## 🚧 Limitations
The program has the following potential issues or limitations:

*   The CLL is not dynamically resized, so it may become full and unable to insert new elements
*   The program does not handle duplicate elements or elements that are not in the CLL
*   The program does not perform any error checking on the inputs
*   The program uses a simple insertion algorithm that may not be efficient for large CLLs
*   The program uses a linear search algorithm that may not be efficient for large CLLs

## 📝 Notes
This program demonstrates the implementation of a Circularly Linked List (CLL) data structure and its associated functions. The CLL is a type of linked list where the last node points back to the first node, forming a circular structure. The program provides functions to insert elements at the beginning of the CLL, search for the position of a specific element, and traverse the list to display its elements. However, the program has some limitations and does not handle certain edge cases, such as inserting duplicate elements or searching for elements that are not in the CLL.