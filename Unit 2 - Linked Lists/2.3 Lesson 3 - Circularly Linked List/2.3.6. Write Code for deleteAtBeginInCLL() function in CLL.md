# 2.3.6. Write Code for deleteAtBeginInCLL() function in CLL
## 📋 Overview
This C program implements a Circularly Linked List (CLL) data structure, providing functions to create a CLL, insert nodes at the beginning, delete nodes from the beginning, and traverse the list. The main focus of this documentation is on the `deleteAtBeginInCLL` function, which removes the node at the beginning of the CLL.

## 🎯 Purpose
The purpose of this program is to demonstrate the implementation of a Circularly Linked List and its associated operations, specifically the deletion of a node from the beginning of the list. This is useful for understanding how CLLs work and how to manipulate them in various applications, such as database query results, browser history, or any scenario where a circular structure is beneficial.

## ✨ Key Features
* Implementation of a Circularly Linked List (CLL) data structure
* Function to create a new node with a given data value
* Function to insert a new node at the beginning of the CLL
* Function to delete the node at the beginning of the CLL
* Function to traverse the CLL and print the data values of all nodes

## 🏗️ Data Structures Used
A Circularly Linked List (CLL) is a type of linked list where the last node points back to the first node, forming a circular structure. In this program, a CLL is defined using a `struct node` with two members: `data` to store the node's value and `next` to point to the next node in the list. The `NODE` type is a pointer to `struct node`, used for convenience.

## 🔧 Implementation Details

### Algorithm Explanation
The main algorithm for the `deleteAtBeginInCLL` function involves updating the `next` pointers of the existing nodes to skip the node to be deleted. Here's a step-by-step breakdown:
1. Check if the CLL is empty or contains only one node.
2. If the list is empty, return `NULL`.
3. If the list has only one node, delete the node and return `NULL`.
4. Otherwise, find the last node in the list by traversing the CLL until the `next` pointer points back to the first node.
5. Update the `next` pointer of the last node to point to the second node in the list (i.e., the node after the first node).
6. Delete the first node and return the new first node (i.e., the second node in the original list).

### Functions Documentation
- **`createNodeInCLL`**: `NODE createNodeInCLL(int x)`
  - **Purpose**: Creates a new node with the given data value `x`.
  - **Parameters**: `int x` - the data value to be stored in the new node.
  - **Returns**: A pointer to the newly created node (`NODE` type).
  - **Logic**: Allocates memory for a new node, sets its `data` member to `x`, and sets its `next` pointer to `NULL`.
- **`insertAtBeginInCLL`**: `NODE insertAtBeginInCLL(NODE first, int x)`
  - **Purpose**: Inserts a new node with the given data value `x` at the beginning of the CLL.
  - **Parameters**: `NODE first` - the current first node of the CLL, `int x` - the data value to be stored in the new node.
  - **Returns**: A pointer to the new first node of the CLL (`NODE` type).
  - **Logic**: Creates a new node with `x` as its data value and updates the `next` pointers of the existing nodes to point to the new node.
- **`deleteAtBeginInCLL`**: `NODE deleteAtBeginInCLL(NODE first)`
  - **Purpose**: Deletes the node at the beginning of the CLL.
  - **Parameters**: `NODE first` - the current first node of the CLL.
  - **Returns**: A pointer to the new first node of the CLL (`NODE` type), or `NULL` if the list becomes empty.
  - **Logic**: Updates the `next` pointers of the existing nodes to skip the node to be deleted and frees the memory allocated to the deleted node.
- **`traverseListInCLL`**: `void traverseListInCLL(NODE first)`
  - **Purpose**: Traverses the CLL and prints the data values of all nodes.
  - **Parameters**: `NODE first` - the current first node of the CLL.
  - **Returns**: None (void function).
  - **Logic**: Follows the `next` pointers to visit each node in the CLL and prints the data value of each node.

## 💻 Compilation & Usage

### Compilation
```bash
gcc "2.3.6. Write Code for deleteAtBeginInCLL() function in CLL.c" -o 2.3.6. Write Code for deleteAtBeginInCLL() function in CLL
```

### Running the Program
```bash
./2.3.6. Write Code for deleteAtBeginInCLL() function in CLL
```

## 📥 Input/Output

### Input
The program does not have any explicit input functions. The `insertAtBeginInCLL` function inserts a new node with a given data value `x` at the beginning of the CLL.

### Output
The `traverseListInCLL` function prints the data values of all nodes in the CLL to the console. The `deleteAtBeginInCLL` function also prints the data value of the deleted node to the console.

## 📊 Complexity Analysis
- **Time Complexity**:
  - `createNodeInCLL`: O(1) - constant time complexity.
  - `insertAtBeginInCLL`: O(n) - linear time complexity, where n is the number of nodes in the CLL.
  - `deleteAtBeginInCLL`: O(n) - linear time complexity, where n is the number of nodes in the CLL.
  - `traverseListInCLL`: O(n) - linear time complexity, where n is the number of nodes in the CLL.
- **Space Complexity**:
  - `createNodeInCLL`: O(1) - constant space complexity.
  - `insertAtBeginInCLL`: O(1) - constant space complexity.
  - `deleteAtBeginInCLL`: O(1) - constant space complexity.
  - `traverseListInCLL`: O(1) - constant space complexity.

## 🧪 Example Run
```c
int main() {
    NODE first = NULL;
    first = insertAtBeginInCLL(first, 10);
    first = insertAtBeginInCLL(first, 20);
    first = insertAtBeginInCLL(first, 30);
    traverseListInCLL(first); // prints: 30 --> 20 --> 10 -->
    first = deleteAtBeginInCLL(first);
    traverseListInCLL(first); // prints: 20 --> 10 -->
    return 0;
}
```

## ⚠️ Edge Cases & Error Handling
The program handles the following edge cases:
- If the CLL is empty, the `insertAtBeginInCLL` function creates a new node and sets it as the first node.
- If the CLL has only one node, the `deleteAtBeginInCLL` function deletes the node and returns `NULL`.
- If the CLL is empty, the `traverseListInCLL` function does not print anything.

## 🚧 Limitations
The program has the following potential issues or limitations:
- The program does not handle memory allocation failures. If `malloc` fails to allocate memory, the program will crash.
- The program does not handle node deletion failures. If the node to be deleted is not found, the program will crash.
- The program does not have any bounds checking mechanisms. If the CLL is very large, the program may run out of memory or crash.

## 📝 Notes
This program demonstrates the implementation of a Circularly Linked List and its associated operations. It provides a basic understanding of how CLLs work and how to manipulate them. However, it is essential to consider the potential issues and limitations mentioned above when using this program in a real-world application.