# 2.1.5. Program to Insert an element at Begin and Count number of Nodes in Singly Linked List

## Description
This program implements 2.1.5. Program to Insert an element at Begin and Count number of Nodes in Singly Linked List

## File Location
`Unit 2 - Linked Lists/2.1 Lesson 1 - Singly Linked List/2.1.5. Program to Insert an element at Begin and Count number of Nodes in Singly Linked List.c`

## Code Preview
```c
struct node {
	int data;
	struct node *next;
};
typedef struct node *NODE;

NODE createNode() {
	struct node* temp=malloc(sizeof(struct node));
	temp->next=NULL;
	return temp;
}

NODE insertAtBegin(NODE first, int x) {
	struct node* temp=malloc(sizeof(struct node));
	temp->data=x;
	if(first==NULL){
		first=temp;
		return first;
	}
	temp->next=first;
	first=temp;
	return first;
	
}

int count(NODE first) {
	int ans=0;
	struct node* temp=first;
	while(temp){
		ans++;
		temp=temp->next;
	}
	return ans;
}

void traverseList(NODE first) {
	NODE temp = first;
	while (temp != NULL) {
		printf("%d --> ",temp -> data);
		temp = temp -> next;
	}
	printf("NULL\n");
}


```

## Compilation
```bash
gcc "2.1.5. Program to Insert an element at Begin and Count number of Nodes in Singly Linked List.c" -o program
```

## Usage
```bash
./program
```

## Notes
- This is part of a Data Structures course (CS201B)
- Auto-generated documentation - Full detailed documentation will be generated by the CI workflow
