# 2.1.4. Write a C program to Create a Singly Linked List

## Description
This program implements 2.1.4. Write a C program to Create a Singly Linked List

## File Location
`Unit 2 - Linked Lists/2.1 Lesson 1 - Singly Linked List/2.1.4. Write a C program to Create a Singly Linked List.c`

## Code Preview
```c
struct node {
	int data;
	struct node *next;
};
typedef struct node *NODE;

NODE createNode() {
	struct node* temp=malloc(sizeof(struct node));
	temp->next=NULL;
	return temp;
}

NODE addNodes(NODE first, int x) {
	if(first==NULL){
		struct node* t=createNode();
		t->data=x;
		t->next=NULL;
		first=t;
		return first;
	}else {
		struct node*temp=first;
		while(temp->next){
			temp=temp->next;
		}
		struct node* t=createNode();
		t->data=x;
		t->next=NULL;
		temp->next=t;
		return first;
	}
}

void traverseList(NODE first) {
	struct node* temp=first;
	while(temp){
		printf("%d --> ",temp->data);
		temp=temp->next;
	}
	printf("NULL\n");
	
}


```

## Compilation
```bash
gcc "2.1.4. Write a C program to Create a Singly Linked List.c" -o program
```

## Usage
```bash
./program
```

## Notes
- This is part of a Data Structures course (CS201B)
- Auto-generated documentation - Full detailed documentation will be generated by the CI workflow
