# 2.1.6. Write a C program to Insert an element at End in Singly Linked List
## 📋 Overview
This C program implements a singly linked list data structure and provides a function to insert an element at the end of the list. The program defines a node structure, creates a new node, inserts a new node at the end of the list, and traverses the list to print its elements.

## 🎯 Purpose
The purpose of this program is to demonstrate the implementation of a singly linked list and the insertion of elements at the end of the list. This program solves the problem of efficiently inserting elements at the end of a linked list, which is a common operation in many applications.

## ✨ Key Features
* Implementation of a singly linked list data structure
* Function to create a new node
* Function to insert a new node at the end of the list
* Function to traverse the list and print its elements
* Dynamic memory allocation for node creation

## 🏗️ Data Structures Used
The program uses a singly linked list data structure, which is a linear data structure in which each node contains a value and a reference (or "link") to the next node in the sequence. The node structure is defined as follows:
```c
struct node {
    int data;
    struct node *next;
};
typedef struct node *NODE;
```
The `next` pointer in each node points to the next node in the list, allowing for efficient insertion and deletion of nodes at any position in the list.

## 🔧 Implementation Details

### Algorithm Explanation
The program uses the following algorithms:

1. **Node creation**: The `createNode()` function uses dynamic memory allocation to create a new node.
2. **Insertion at the end**: The `insertAtEnd()` function uses a traversal algorithm to find the last node in the list and then inserts a new node at the end.
3. **Traversal**: The `traverseList()` function uses a simple iterative algorithm to traverse the linked list and print its elements.

### Functions Documentation
- **`createNode()`**: 
  - **Purpose**: Creates a new node in the linked list.
  - **Parameters**: None
  - **Returns**: A pointer to the newly created node (`NODE`)
  - **Logic**: The function dynamically allocates memory for a new node using `malloc()` and initializes the `next` pointer to `NULL`.
- **`insertAtEnd(NODE first, int x)`**:
  - **Purpose**: Inserts a new node with the given value `x` at the end of the linked list.
  - **Parameters**:
    - `first`: A pointer to the first node in the linked list (`NODE`)
    - `x`: The value to be inserted at the end of the list (`int`)
  - **Returns**: A pointer to the first node in the updated linked list (`NODE`)
  - **Logic**: If the list is empty (i.e., `first` is `NULL`), a new node is created using `createNode()` and its `data` member is set to `x`. If the list is not empty, a new node is created using `createNode()` and its `data` member is set to `x`. The function then traverses the list to find the last node by following the `next` pointers. Once the last node is found, its `next` pointer is set to the new node, effectively inserting the new node at the end of the list.
- **`traverseList(NODE first)`**:
  - **Purpose**: Prints the elements of the linked list.
  - **Parameters**:
    - `first`: A pointer to the first node in the linked list (`NODE`)
  - **Returns**: None
  - **Logic**: The function iterates through the list, printing the `data` member of each node, until it reaches the end of the list (i.e., `NULL`).

## 💻 Compilation & Usage

### Compilation
```bash
gcc "2.1.6. Write a C program to Insert an element at End in Singly Linked List.c" -o 2.1.6. Write a C program to Insert an element at End in Singly Linked List
```

### Running the Program
```bash
./2.1.6. Write a C program to Insert an element at End in Singly Linked List
```

## 📥 Input/Output

### Input
The program expects the following input:
* The value `x` to be inserted at the end of the list (passed as an argument to the `insertAtEnd()` function)

### Output
The program produces the following output:
* The updated linked list with the new node inserted at the end (returned by the `insertAtEnd()` function)
* The elements of the linked list (printed by the `traverseList()` function)

## 📊 Complexity Analysis
- **Time Complexity**:
  - `createNode()`: O(1) (constant time) since it only involves dynamic memory allocation and initialization.
  - `insertAtEnd()`: O(n) (linear time) since it involves traversing the list to find the last node, where n is the number of nodes in the list.
  - `traverseList()`: O(n) (linear time) since it involves iterating through the entire list.
- **Space Complexity**: O(n) since the program uses dynamic memory allocation to create new nodes, where n is the number of nodes in the list.

## 🧪 Example Run
```
int main() {
    NODE first = NULL;
    first = insertAtEnd(first, 10);
    first = insertAtEnd(first, 20);
    first = insertAtEnd(first, 30);
    traverseList(first);  // Output: 10 --> 20 --> 30 --> NULL
    return 0;
}
```
This example creates a linked list with three nodes containing the values 10, 20, and 30, and then prints the elements of the list using `traverseList()`.

## ⚠️ Edge Cases & Error Handling
The program does not handle the following edge cases:
* **Memory allocation failure**: If `malloc()` fails to allocate memory for a new node, the program will crash or produce undefined behavior.
* **NULL pointer dereference**: If the `first` pointer is `NULL` when calling `insertAtEnd()` or `traverseList()`, the program will crash or produce undefined behavior.
* **Integer overflow**: If the `data` member of a node exceeds the maximum value that can be represented by an `int`, the program may produce incorrect results or crash.

To handle these edge cases, the program should include error checking and handling code, such as checking the return value of `malloc()` and handling `NULL` pointer dereferences.

## 🚧 Limitations
The program has the following potential issues or limitations:
* **Memory leaks**: If the program does not properly free the memory allocated for nodes when they are no longer needed, it may cause memory leaks.
* **Inefficient insertion**: The `insertAtEnd()` function has a time complexity of O(n) due to the traversal required to find the last node. For large lists, this can be inefficient.
* **Limited node capacity**: The program uses a fixed-size `int` to store the `data` member of each node. If the program needs to store larger values, it may need to use a different data type.

## 📝 Notes
The program demonstrates a basic implementation of a singly linked list and the insertion of elements at the end of the list. However, it may need to be modified or extended to handle edge cases, improve efficiency, or support additional operations.