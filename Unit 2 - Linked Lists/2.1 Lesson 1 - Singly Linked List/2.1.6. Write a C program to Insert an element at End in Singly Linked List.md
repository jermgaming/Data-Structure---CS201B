# 2.1.6. Write a C program to Insert an element at End in Singly Linked List

## Description
This program implements 2.1.6. Write a C program to Insert an element at End in Singly Linked List

## File Location
`Unit 2 - Linked Lists/2.1 Lesson 1 - Singly Linked List/2.1.6. Write a C program to Insert an element at End in Singly Linked List.c`

## Code Preview
```c
struct node {
	int data;
	struct node *next;
};
typedef struct node *NODE;

NODE createNode() {
	NODE temp;
	temp = (NODE) malloc(sizeof(struct node));
	temp -> next = NULL;
	return temp;
}

NODE insertAtEnd(NODE first, int x) {
	if(!first){
		first=createNode();
		first->data=x;
		return first;
	}
	struct node* temp=createNode();
	temp->data=x;
	struct node* copyFirst=first;
	while(copyFirst->next){
		copyFirst=copyFirst->next;
	}
	copyFirst->next=temp;
	return first;
}

void traverseList(NODE first) {
	struct node* temp=first;
	while(temp){
		printf("%d --> ",temp->data);
		temp=temp->next;
	}
	printf("NULL\n");
}


```

## Compilation
```bash
gcc "2.1.6. Write a C program to Insert an element at End in Singly Linked List.c" -o program
```

## Usage
```bash
./program
```

## Notes
- This is part of a Data Structures course (CS201B)
- Auto-generated documentation - Full detailed documentation will be generated by the CI workflow
