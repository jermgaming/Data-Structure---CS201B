# 2.6.8. Swapping
## 📋 Overview
This C program is designed to swap the kth node from the beginning and the kth node from the end of a singly linked list. The program takes the number of nodes, the values of the nodes, and the value of k as input, and then prints the modified linked list after swapping the nodes.

## 🎯 Purpose
The purpose of this program is to demonstrate the implementation of a singly linked list and the swapping of nodes within it. The program solves the problem of rearranging the nodes in a linked list based on a given input, which can be useful in various applications such as data manipulation and algorithmic exercises.

## ✨ Key Features
* Creates a singly linked list with a specified number of nodes
* Inserts nodes at the end of the linked list
* Prints the values of all nodes in the linked list
* Swaps the kth node from the beginning and the kth node from the end
* Handles user input for the number of nodes, node values, and the value of k

## 🏗️ Data Structures Used
The program uses a singly linked list, which is a linear data structure where each node contains a value and a reference (i.e., a "link") to the next node in the sequence. The linked list is defined as follows:
```c
struct node {
    int data;
    struct node* next;
};
```
Each node in the linked list has two members: `data` to store the value of the node, and `next` to point to the next node in the list.

## 🔧 Implementation Details

### Algorithm Explanation
The program uses the following steps to swap the kth node from the beginning and the kth node from the end:

1. Read the number of nodes (n) in the linked list.
2. Create an empty linked list (head = NULL).
3. Read the values of the nodes (x) and insert them at the end of the linked list using the `insertTail` function.
4. Read the value of k, which specifies the nodes to be swapped.
5. Traverse the linked list to find the kth node from the beginning (t1) and the kth node from the end (t2).
6. Swap the data values of the kth node from the beginning and the kth node from the end.
7. Print the modified linked list using the `print` function.

### Functions Documentation
#### `createNode(int data)`
* **Purpose**: Creates a new node with the given data and returns a pointer to the new node.
* **Parameters**: `int data` - the value to be stored in the new node.
* **Returns**: `struct node*` - a pointer to the newly created node.
* **Logic**: The function allocates memory for a new node using `malloc`, initializes the `data` member with the given value, and sets the `next` member to NULL.

#### `insertTail(struct node* head, int data)`
* **Purpose**: Inserts a new node with the given data at the end of the linked list.
* **Parameters**: `struct node* head` - the head of the linked list, `int data` - the value to be stored in the new node.
* **Returns**: `struct node*` - the head of the modified linked list.
* **Logic**: The function traverses the linked list to find the last node, creates a new node with the given data, and appends it to the end of the list.

#### `print(struct node* head)`
* **Purpose**: Prints the values of all nodes in the linked list.
* **Parameters**: `struct node* head` - the head of the linked list.
* **Returns**: None (void).
* **Logic**: The function traverses the linked list and prints the `data` member of each node.

#### `main()`
* **Purpose**: The entry point of the program, responsible for reading input, creating the linked list, swapping the nodes, and printing the modified list.
* **Parameters**: None (void).
* **Returns**: `int` - the program's exit status (0 indicates successful execution).
* **Logic**: The function reads the number of nodes, node values, and the value of k, creates the linked list, swaps the nodes, and prints the modified list.

## 💻 Compilation & Usage

### Compilation
```bash
gcc "2.6.8. Swapping.c" -o 2.6.8. Swapping
```

### Running the Program
```bash
./2.6.8. Swapping
```

## 📥 Input/Output

### Input
The program expects the following inputs:
* The number of nodes (n) in the linked list.
* The values of the nodes (x) in the linked list.
* The value of k, which specifies the nodes to be swapped.

### Output
The program produces the following output:
* The modified linked list after swapping the kth node from the beginning and the kth node from the end.

## 📊 Complexity Analysis
* **Time Complexity**: The time complexity of the program is O(n), where n is the number of nodes in the linked list. This is because the program traverses the linked list to insert nodes, find the kth node from the beginning and the end, and print the modified list.
* **Space Complexity**: The space complexity of the program is O(n), where n is the number of nodes in the linked list. This is because the program uses a linked list to store the nodes, which requires O(n) space.

## 🧪 Example Run
```
Enter the number of nodes: 5
Enter the values of the nodes: 1 2 3 4 5
Enter the value of k: 2
Modified linked list: 1 4 3 2 5
```

## ⚠️ Edge Cases & Error Handling
The program does not handle the following edge cases:
* If the input value of k is greater than the number of nodes in the linked list, the program will access nodes that do not exist, leading to undefined behavior.
* If the input value of k is less than or equal to 0, the program will not swap the nodes correctly.
* If the input values of the nodes are not valid integers, the program will not work correctly.

To handle these edge cases, the program should add error checking code to validate the input values and handle any errors that may occur.

## 🚧 Limitations
The program has the following limitations:
* The program uses a singly linked list, which can be less efficient than other data structures like arrays or doubly linked lists for certain operations.
* The program does not handle edge cases or errors, which can lead to undefined behavior or incorrect results.
* The program uses a simple swapping algorithm, which may not be efficient for large linked lists.

To address these limitations, the program could be modified to use a more efficient data structure, add error checking code, or use a more efficient swapping algorithm.

## 📝 Notes
The program is designed to demonstrate the implementation of a singly linked list and the swapping of nodes within it. The program can be used as a starting point for more complex linked list operations and can be modified to handle edge cases and errors.